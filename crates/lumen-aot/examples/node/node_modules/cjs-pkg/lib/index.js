'use strict';
// A CommonJS package: relative require, JSON require, an optional dependency behind try.
const util = require('./util');
exports.add = function add(a, b) { return a + b; };
exports.data = require('../data.json');
exports.util = util.name;
exports.dirname = __dirname;
try {
  require('aot-node-not-installed');
  exports.optional = 'present';
} catch (e) {
  exports.optional = 'missing';
}
